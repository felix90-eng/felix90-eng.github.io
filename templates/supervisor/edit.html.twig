{% extends 'base.html.twig' %}

{% block title %}Edit Supervisor{% endblock %}

{% block body %}
    <h1>Edit Supervisor</h1>

    {{ include('supervisor/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('supervisor_index') }}">back to list</a>

    {{ include('supervisor/_delete_form.html.twig') }}
{% endblock %}

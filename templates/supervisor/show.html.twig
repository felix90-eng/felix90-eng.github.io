{% extends 'base.html.twig' %}

{% block title %}Supervisor{% endblock %}

{% block body %}
    <h1>Supervisor</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ supervisor.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ supervisor.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('supervisor_index') }}">back to list</a>

    <a href="{{ path('supervisor_edit', {'id': supervisor.id}) }}">edit</a>

    {{ include('supervisor/_delete_form.html.twig') }}
{% endblock %}

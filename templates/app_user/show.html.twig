{% extends 'base.html.twig' %}

{% block title %}AppUser{% endblock %}

{% block body %}

    <h1>AppUser</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ app_user.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ app_user.email }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ app_user.password }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_user_index') }}">back to list</a>

    <a href="{{ path('app_user_edit', {'id': app_user.id}) }}">edit</a>
    <a href="{{ path('app_user_edit', {'id': app_user.id}) }}">Add role</a>

    {{ include('app_user/_delete_form.html.twig') }}
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}CarTrip{% endblock %}

{% block body %}
    <h1>CarTrip</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ car_trip.id }}</td>
            </tr>
            <tr>
                <th>Trip</th>
                <td>{{ car_trip.trip }}</td>
            </tr>
            <tr>
                <th>Amountprice</th>
                <td>{{ car_trip.amountprice }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('car_trip_index') }}">back to list</a>

    <a href="{{ path('car_trip_edit', {'id': car_trip.id}) }}">edit</a>

    {{ include('car_trip/_delete_form.html.twig') }}
{% endblock %}

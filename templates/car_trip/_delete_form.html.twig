<form method="post" action="{{ path('car_trip_delete', {'id': car_trip.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ car_trip.id) }}">
    <button class="btn">Delete</button>
</form>
